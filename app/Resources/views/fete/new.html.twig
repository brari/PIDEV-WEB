{% extends 'base.html.twig' %}
{% block body %}
    <div style="height: 30px; background-color: #9e1847">
        <center><ul>
                <a>FORMULAIRE</a>
                <a>PRODUITS</a>
                <a>LIVRAISON</a>
            </ul></center>
    </div>
    <div class="wthree-form">  <h1 style="color: #a94442">Fete creation</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" id="create" />

        {#  <select>
              {% for user in users %}
              <option>{{  user.nom }} {{ user.prenom }}</option>
              {% endfor %}
          </select> #}
        {{ form_end(form) }}</div>
    </br>
    </br>
    {# <input type="submit"value="ajouter des produits" id="b"/>

    <form id="f" style="display:none">
        {% for prod in prods %}
        <fieldset style="border: 2px solid #9e1847; "  >
            <div class="right_col" id="d">
                <center><img src="{{ asset(prod.getImage()) }}" style=" background-position:center center;display:block;background-repeat:no-repeat;background-size:cover;height:200px;position:relative;width:262px;"/>
                <h2 style="font-size:18px;margin:0 0 20px 0;padding:0;font-weight: bold;">{{ prod.getNompro() }}</h2></center>

            </div>
  <div class="content_area">
      <dl style="margin:0;padding:0">
          <dd style="float:left;line-height:36px">Categorie</dd>
          <dt style="border-bottom:1px dashed #e9e9e9;line-height:36px;text-align:right">{{ prod.getCategoriepro() }}</dt>
          <dd style="float:left;line-height:36px">Details</dd>
          <dt style="border-bottom:1px dashed #e9e9e9;line-height:36px;text-align:right">{{ prod.getDetailspro() }}</dt>
          <dd style="float:left;line-height:36px">Prix</dd>
          <dt style="border-bottom:1px dashed #e9e9e9;line-height:36px;text-align:right">{{ prod.getPrixpro() }} DT </dt>
      </dl>
  </div>

            <input type="checkbox" name="produit" id="red" VALUE="{{prod.getIdpro}}"/>
        </fieldset>
            </br>
            </br>
        {% endfor %}
  <input type="submit" id="h" />
      </form> #}
    <br>
    <br>
    <center><b><a href="{{ path('crud_index') }}">Retour à la liste </a></b></center>

{% endblock %}


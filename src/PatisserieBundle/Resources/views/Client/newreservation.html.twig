{% extends 'base.html.twig' %}

{% block body %}
    <h1>Reservation creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter" onclick="return confirm('Ajouter la reservation?')"/>
    {{ form_end(form) }}

   {# <ul>
        <li>
            {% if app.user %}
            <a href="{{ path('reservation_index',{'id':app.user.id}) }}">Abandonner</a>
            {% endif %}
        </li>
    </ul> #}
{% endblock %}

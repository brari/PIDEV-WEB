<div class="row">
    <div class="col-xs-12">
        <p>Êtes vous sur de vouloir activer le sujet "<strong>{{ entity.title }} ?</strong>"</p>
        <form class="active-subject-form" id="subject" method="post" action="{{ path('subject_active', { 'subject': entity.id }) }}" >
            {{ form_widget(form) }}
        </form>
    </div>
</div>
